package service

import "testing"

func TestServicePackageCompiles(t *testing.T) {
	// This package currently contains documentation and composition boundaries.
	// Keep a small smoke test so the package is included in go test output.
}
