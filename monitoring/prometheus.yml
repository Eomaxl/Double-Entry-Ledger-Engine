global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "ledger-engine"
    metrics_path: /metrics
    static_configs:
      - targets: ["ledger-engine:8080"]

  - job_name: "node-exporter"
    static_configs:
      - targets: ["node-exporter:9100"]
